#pragma once

namespace global {
	/*
	static float MIN_RADIUS = 30.0f;
	static float MAX_RADIUS = 60.0f;
	static float MIN_MASS =	1.0f;
	static float MAX_MASS =	3.0f;
	static float MIN_BOUNCE = 0.80f; //0.6f;
	static float MAX_BOUNCE	= 0.95f; //.99f;
	static float GRAVITY = .0001;

	static int COLOR_SCHEME = 0;
	*/

	extern float MIN_RADIUS;
	extern float MAX_RADIUS;
	extern float MIN_MASS;
	extern float MAX_MASS;
	extern float MIN_BOUNCE;
	extern float MAX_BOUNCE;
	extern float GRAVITY;

	extern int COLOR_SCHEME;
}